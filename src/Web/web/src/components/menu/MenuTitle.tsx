import { Typography } from "@mui/material";

export function MenuTitle(): JSX.Element {
  return (
    <Typography sx={{ padding: "5px" }} variant="h5">
      PARKING SLOTS RESERVATIONS
    </Typography>
  );
}
